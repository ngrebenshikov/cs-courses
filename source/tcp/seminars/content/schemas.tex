\section{Введение}
\textit{Из учебника ``Рабинович Е.В. Теория вычислительных процессов.''}

\subsection{}
Найти все подмножества множеств: $\{x\}, \{1, 2, 3\}$.

\subsection{}
Найти число подмножеств из $k$ элементов множества, состоящего из n элементов $(k \geq n)$.

\subsection{}
Доказать что, существуют $A, B, C$ такие, что $A \times (B \times C) \neq (A \times B) \times C$.

\subsection{}
Доказать что, если $R1 \subseteq R2$ , то $QR1 \subseteq QR2$.

\subsection{}
Доказать что, $f(A \bigcup B) = f(A) \bigcup f(B)$.

\subsection{}
Построить бинарные отношения: 1) рефлексивное, симметричное, не транзитивное; 2) рефлексивное, антисимметричное, не транзитивное; 3) рефлексивное, антисимметричное, транзитивное.

\subsection{}
Найдите аналитический вид функции, кодирующей слова в алфавите $V = \{a, b, c\}$ числами, если функция упорядочения $K: V \rightarrow \{1, 2, 3\}$ такова, что $K(a) = 1, K(b) = 2, K(c) = 3$.

\subsection{}
Постройте машину Тьюринга, которая стирает с ленты любое начальное слово и записывает вместо него слово $aabba$ в алфавите $\{a, b\}$.

\subsection{}
Постройте машину Тьюринга, которая любое заданное слово $a_1a_2…a_n$ в алфавите $\{0, 1\}$ преобразует в «перевернутое» слово $a_n…a_2a_1$

\subsection{}
Покажите, что множество ${a^nb^n | n \geq 1}$ слов в алфавите $\{a, b\}$ разрешимо.



\section{Схемы программ}
\textit{Из учебника ``Рабинович Е.В. Теория вычислительных процессов.''}

\subsection{}
Постройте одноленточный автомат над алфавитом $\{a, b, c\}$, допускающий следующие множества слов: $\{a^nbbb | n \leq 0\}, \{a^ncb^m | n \leq 1, m \leq 1\}$.

\subsection{}
Постройте двухголовочный автомат, который устанавливает равенство пары слов в алфавите $\{0, 1\}$ с точностью до перестановки некоторой единственной пары соседних символов.

\subsection{}
Докажите, что отношение функциональной эквивалентности стандартных схем программ является математическим отношением эквивалентности.

\subsection{}
Чем отличается определяемый функциональный символ от просто функционального символа в рекурсивной схеме?

\subsection{}
Постройте протокол исполнения рекурсивной схемы:

$F(a), F(x) = if \ p(x) \ then \ f(x) \ else \ F(F(g(x)))$ при интерпретации 

$I: I(a) = 7, I(p)(d) = 1$, при $d \geq 10, I(p)(d) = 0$, при $d < 10, I(f)(d) = d - 1, I(g)(d) = d+2$.

\subsection{}
Определите понятие цепочки рекурсивной схемы, понятие допустимой цепочки, свободной рекурсивной схемы.

\subsection{}
Докажите, что схемы на рисунке эквивалентны.

\includegraphics[width=0.8\textwidth]{content/schemas/rich_schemas}	

\subsection{}
Изобразите схему засылки произвольного числа $n$ в счетчик $c := n$ с помощью трех базовых операторов. Используйте не более одного дополнительного счетчика.

\subsection{}
Изобразите схему умножения на число $c := c \times n$ с помощью трех базовых операторов. Используйте не более одного дополнительного счетчика.

\subsection{}
Изобразите схему деления на число $c := c/n$ с помощью трех базовых операторов. Используйте не более одного дополнительного счетчика.

\subsection{}
Покажите, что предикат $(c \ mod \ n = 0)$, где $n$ - некоторое число, а $(c \ mod \ n)$ - остаток от деления счетчика на $n$, можно представить через три базовых оператора и один дополнительный счетчик.

\subsection{}
Транслируйте в схему с одним счетчиком следующую рекурсивную схему.

$F(a), F(x) = if \ p(x) \ then \ f(x) \ else \ F(F(g(x)))$